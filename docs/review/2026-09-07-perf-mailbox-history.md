# Mailbox history performance review

Accepted production change is `44ffbb52`, with tests `ca7f498c` and `5479ed6a`. It adds the delivered-only partial index and replaces listMessages with one ordered compound statement. Statement caching stays unchanged. [Measurements and costs](../performance/2026-09-07-exhaustive/c10-mailbox-history-results.md) contain the decision evidence.

The NULL and non-NULL delivery predicates partition all rows without duplication. Both placeholders bind the same Bee, and a single statement retains the existing snapshot and read-your-writes semantics. Rowid supplies per-Bee id order; the plan proves MERGE UNION ALL without a temporary sort. Public PK reads serve as the full-row oracle, including inside a transaction that moves a message to the delivered arm and cancels another. Rollback restores the exact rows. A deliberately different second Bee catches binding the wrong arm.

The delivered partial cannot serve pending predicates. Twelve verbatim production statements plus one test-only delivered-arm probe retain the expected access paths. Mini's 20 pending plus 100,000 delivered negative control holds at 0.024 ms CPU. Real source checks identify the three callers as mailbox RPC, automatic title context and spawn fallback. Untitled active does not imply a small history, so the measured giant-read regression applies wherever such a caller encounters one.

Migration proof covers reinstall on open, pre-v8 schema without urgency, raw old-build inserts and deliveries, schema-version stability and integrity_check. Only v1-era columns appear in the new declaration. The index build can scan all existing mailbox rows and can increase open cost and retained storage. Public delivery maintains one new index entry. These are accepted and quantified, not called free.

The initial test oracle used casts and scalar coercions. The frozen cleanup uses checked text/number readers and validates the integer encoded in metadata text. It also strengthens the inside-transaction oracle to all fields. The final source hashes are schema 5521ca43cd2db4987d811aa28a21174618cffbdcf8f44793f6896b13c07aeb7e, store c218447aa682e9974110e353c335ae5ebdc9d2dba9a7643509bbc4a8ec1757bf, test 6d4be4bdf5135c12b6aa36b0cb828ad2a665ce9c8e9f6660bed32615a17f9d0f.

Independent ruler review found that EXPLAIN ran after timed writes. The corrected version runs it on the original read fixture, closing exclusive store connections first and verifying full state after reopen. A real locked-database smoke failure is retained. Read and write sampling counts, exact audit growth, module URLs and warmed reopen scope are explicit. Final reviewer found no correctness blocker, with only the post-inspection wording clarification carried into the result document.

Standalone Mini build, all v2 typechecks, core 230/230, daemon/CLI 368 plus one skip passed. Final tests-only cleanup passed five cases and core typecheck. The exact integrated source c01f7733 passed build, all v2 typechecks, core 230/230 and daemon/CLI 368 plus one platform skip. The recorded source proof matches every measured runtime/package hash. No new legacy full-suite claim is made; the previously recorded 67 identical control failures remain documented.
