# Automatic-title memory measurement review

The revised retained-memory ruler e4a1db87b8290ab9217533171d7cc5883aec54305770658c3b3f7789984f191b is approved for controlled measurement. This approves its stated whole-process scope, not a candidate optimization or per-cache memory claim.

The [draft review](../performance/2026-09-07-exhaustive/designs/autotitle-architecture/retained-ruler-v1-review.md) required seven corrections. Full diff review confirms a pre-scan constructed phase, separate first-scan label, serial ABBA processes, explicit scan budgets, scoped quiet-state equality, consistent yielding between GC calls, and reliable normal error cleanup. Snapshot-mode readings are diagnostic-only and cannot produce derived differences. The claimed private-Map reflection access and retained-total bound are removed.

Both revised Studio smoke reports pass their structural assertions. All twelve original and revised snapshot artifacts retain exact hashes after lossless archive compression. The author corrected a handoff overclaim: the roughly 73 KB first-scan change in the revised A/A smoke is whole-process occupancy, not candidate-cache retention. Those baseline roots contain no candidate cache.

Mini smoke and canonical A/A runs also pass on distinct clean 322815d9 roots. Canonical mode=none uses four fresh serial processes in order 0,1,1,0, each with 1000 small backoff bees plus one 100k-message backoff bee and 32 scans. Warm heapUsed ranges from 6,854,680 to 6,855,056 B. Warm RSS ranges from 825,049,088 to 866,369,536 B. First-scan heapUsed is about 11.59 MB and later falls to about 6.85 MB, so even the fixed procedure exposes changes in collection behavior. A/A variability is descriptive, not a guarantee that treatment collection artifacts cancel. Treat process readings together with implementation-specific cache-bound tests and offline retaining paths.

Core Unit1 bc6554b6 is separately reviewed by the parent. It adds result types, one cached aggregate and tests across three paths. Count/max combines pending and delivered arms, so delivery does not invalidate membership. The method returns transaction_open before statement lookup. Existing writers, transaction wrappers, indexes and schema are untouched. Rollback/reused-id, caught nested post-insert error, deletion/recreation, isolation and read-only assertions are covered by the real-store suite. Parent Mini build and all v2 typechecks pass, and all 237 Core tests pass. The independent review also found no blocker and passed the seven focused tests. Its non-blocking follow-up covers nested transaction guard behavior, a delivered global maximum, explicit native unsafe-integer errors and comparison-limit documentation. That follow-up is assigned in a separate isolated worktree. Unit2 is authorized in the isolated candidate lane; no autoTitle cache is integrated or accepted yet.
