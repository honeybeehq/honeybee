# Cell capture no-tags review

Production d58a2949 adds one argument to the temporary shared clone. No public command shape, report type, branch update, retry, merge/rebase option, CAS or cleanup path changes. Subsequent scratch operations receive object IDs; branch mutations occur only in the origin under fully qualified refs. The clone's remote.origin.tagOpt config dies with the temporary repository. The shared alternate provides objects independently of copied tag names. The two fetch boundaries were already no-tags.

The independent [review](../performance/2026-09-07-exhaustive/designs/honeybee-cell-no-tags-review.md) and the parent read found no semantic blocker. The tests cover real native tag objects and all three affected engines. Both source-tag and tagless inputs preserve complete reports and deterministic trees/parents/SHAs. There is no dedicated rebase-conflict tag fixture; existing rebase and merge-conflict tests plus the unchanged abort mechanics cover the current change, and the complete Cell suite remains required. Historical Git versions outside the tested2.39.5/2.52.0 pair are not newly claimed verified.

The initial Mini test failed because argv[0] is an absolute executable path there. The correction compares its basename and keeps every remaining argument exact. It does not relax a production behavior assertion. The corrected full Cell suite passes72 with one platform skip. Frozen source hashes and exact failures are retained.

The ruler update derives storage-clone flags from the actual production trace, validates a closed shape and requires either all source tags or zero accordingly. It preserves the raw command and resource evidence. This avoids measuring old clone behavior as though it represented the candidate. Seven native/unit tests and a build pass for the updated tooling.

[Paired measurements](../performance/2026-09-07-exhaustive/cell-no-tags-results.md) compare four independent process runs in ABBA order with twelve headline samples per side/count. At10k tags wall improves40.35%, with repeated clone-CPU and temporary-byte improvements. The zero-tag control remains essentially unchanged and its20-byte config cost is stated. The maximum individual RSS metric is not total process-tree RAM. No maintenance outcomes are filtered. Retained artifacts include both repeats and the exact source/tool hashes.

Final combined 47cd5d6b passes the build, all v2 typechecks, Cell 72 with one platform skip, ruler 7, and daemon/CLI 368 with one skip. The clean remote checkout matches the local production sources; capture.ts matches the measured candidate exactly, and the only other production delta is the accepted C03 cache change. The previously documented67 identical legacy compiled-suite failures remain a separate limitation; these gates do not claim that suite is green.
