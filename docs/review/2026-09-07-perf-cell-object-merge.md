# Cell object-merge review

Reviewed candidate `6984a6a2`, integrated as `86ddf953`, and differential test commits `b383137d`/`3584cd9c`. Accept the clean-merge optimization with its measured fallback cost.

The scratch repository is retained. Moving merge computation into the origin would expose different local Git configuration; this patch keeps the existing configuration boundary and pins scratch HEAD at the target before attribute lookup. It accepts only a successful merge-tree result consisting of a single object ID. All other results retain the original checkout/merge/conflict reporting. Commit-tree uses the same target-first and Cell-second parents, identity environment, and message. Existing CAS publication and finally cleanup remain unchanged.

The main risks were attributes without a populated worktree, multi-base history, filesystem-sensitive names/modes, and changing refusal/ref cleanup behavior. Nine differential fixtures pass on Git 2.52.0 and Apple Git 2.39.5, including union/binary attributes, criss-cross ancestry, gitlinks, symlinks, executable mode, rename/delete conflicts, and APFS case collision. Both engines must attempt merge-tree, and the fallback shim proves interception. Tests compare exact result SHA as well as report, tree, parents, unchanged fixture state, and absent transient refs. The criss-cross fixture asserts two merge bases and semantic file content. This covers two real Git versions plus command absence; it does not emulate every older implementation.

All eight capture outcomes pass exact invariants in smoke, canonical, and 10000-file stress measurements. Clean merge wall p50 falls 400.25→94.86ms canonical and 1615.59→120.46ms stress. Canonical merge conflict increases by14.89ms; the 12-file smoke also shows an11.16ms conflict penalty. The clean-path gain is accepted with that explicit fallback cost. Parent CPU and traced Git duration are scoped separately; total child CPU and whole-daemon responsiveness are still open.

Integration preserves main's empty-rebase fix and Cell-move fences. The frozen combined tree `7d7e6287cb9de1e7dbd5b5914b882b213894a2c9` passed build, all v2 typechecks, core212, daemon299/one skip, CLI59, and adapters70. Capture parity passes there too. Four unrelated Cell delivery tests reproduced the same boot-versus-input-connection timing failure on the pre-Cell-change checkout. Test-only commit `7eb8ecb7` follows the existing HSR test contract: retry only `not_ready`, require acceptance within five seconds, and preserve all turn, process, sandbox, and adoption assertions. Full Cell then passes70/one platform skip, followed by driver-cell typecheck and build. Original failure logs remain.

[Measurements and scope](../performance/2026-09-07-exhaustive/cell-object-merge-results.md). No blocking correctness finding in the reviewed change.
