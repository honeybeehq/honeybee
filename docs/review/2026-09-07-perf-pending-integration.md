# Pending performance integration review

Reviewed the final runtime at `10c25dfc`, final test/tool corrections through `5eb66fad`, and their merge with main `2c3bdf6b`. Runtime changes relative to that main revision are confined to core schema.ts and store.ts. Current main's gateway admission changes are preserved. No production blocker remains in this performance unit after source review, paired correctness assertions and Mini v2 gates. The full legacy compiled suite remains red on both sides, as detailed below.

The independent [array mapping](2026-09-07-perf-pending-metadata.md), [index removal](2026-09-07-perf-pending-index-removal.md), and [combined measurements](../performance/2026-09-07-exhaustive/pending-combined-results.md) record the mechanisms and their tradeoffs. No workload gain is generalized to end-to-end spawning or retained RAM. All original adverse measurements and failed verification attempts remain available.

At the exact measured production source, Mini build, all v2 checks, 225 core tests and 368 daemon/CLI tests passed, with one platform skip. The migration-test refinement separately passed its four-test suite and core typecheck, followed by the repository build. The final proof file hashes match the committed source. No runtime changes followed the final paired captures.

Main's compiled tests had a pre-existing import problem. New shared native TypeScript gateway modules import relative .ts files. Production TypeScript emission rewrites those paths, but the test builder's esbuild pass left them unchanged while emitting only .js files. Compiled tests then failed before executing. The test-tool correction uses a TypeScript AST to locate relative literal import/export/dynamic-import paths ending in .ts and replaces only those literals with .js paths. The helper is a build-stamp input. Source modules remain separate ESM files; native source imports are unchanged. Ordinary strings, comments, URL assets, computed paths and unsupported .mts/.cts paths remain unchanged.

Review narrowed the first helper draft to the .ts files this builder actually emits. A runtime test proves a static import and escaped dynamic import share one emitted module instance. Mini repository build, root source/test typechecks, core typecheck and all 24 helper/gateway tests pass. No raw TypeScript copy or module-identity workaround was introduced.

The full compiled suite passed 3,488 tests, failed 67 and skipped 30. These failures must not be described as a green full-suite gate. The clean Mini test environment lacks tmux, codex and claude on PATH. Failures also include legacy CLI artifact/entry expectations. Unchanged main initially failed much earlier because its gateway module import problem masked whole test files. Parent therefore ran a second control using unchanged main runtime with exactly the same three-file test-builder repair.

That repaired-main control has the identical 67 failure names, with no candidate-only or baseline-only failures. All 693 emitted JavaScript modules under src/tests are byte-identical between repaired main and the candidate. The unmodified-main comparison differs only in gatewayMcp.js, gatewayMcpSeed.js and the added helper test. This establishes that the performance round introduces no additional compiled-suite failure in this environment; it does not establish that all legacy integration behavior works on a fully provisioned host.

The [paired full-suite proof](../performance/2026-09-07-exhaustive/verification/mini-root-main-repaired-control-proof.json) contains every emitted-file hash and failure name. [Final focused source proof](../performance/2026-09-07-exhaustive/verification/mini-final-test-fixes-source.json) records exact file hashes and each command result. Raw full-suite and failed unmodified-control logs are retained alongside it. GitHub Actions listed no runs for this repository at integration time. No push or deployment was performed.
