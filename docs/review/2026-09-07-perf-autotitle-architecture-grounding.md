# Automatic-title architecture grounding review

Neither frozen design is accepted verbatim. Parent review found concrete retry and read-pairing failures in the derived-stamp sketch, and an incomplete deletion/absence contract in the committed-version sketch. A public nested-transaction proof adds a mandatory successful-SQL staging condition. These findings are documented with executable evidence in [architecture grounding](../performance/2026-09-07-exhaustive/autotitle-architecture-grounding.md).

The first quiet-ruler draft also needs corrections before canonical use. Its clocks stop when the dispatcher returns a Promise, its source gate permits any core/daemon delta, and its giant rows are all pending. The revision request preserves that draft and its structural A/A artifacts. No draft timing is accepted as a candidate speedup.

The independent cross-judge, Core-contract addendum, and revised ruler are pending. This commit records completed grounding evidence and provisional parent scores. It does not approve a production cache change. Existing source build/typecheck/Cell/daemon gates remain those recorded for47cd5d6b; this investigation changed no production files.


## Synthesis follow-up

The independent cross-judge and smaller-slice addendum are now complete. Parent and judge agree on a private store-backed, derived-membership first slice with full reads retained for possible launches. The broader Core token and normalized-summary designs remain unimplemented alternatives. [Selected design and baseline](../performance/2026-09-07-exhaustive/autotitle-quiet-design.md) record the decision.

The revised quiet ruler satisfies the measurement review. Final8e4d bytes passed Mini A/A smoke and canonical, with exact state/sidecar/no-provider invariants and raw samples retained. Source treatment acceptance remains pending implementation, full gates and before/after tradeoffs.
