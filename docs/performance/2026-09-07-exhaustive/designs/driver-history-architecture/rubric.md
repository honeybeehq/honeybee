# Parent rubric

Score each0..4. Hard failure on altered default API or load-bearing acknowledgement state.

1. Default behavior and history compatibility are preserved at exact acceptance sites, including refusal/overwrite/lifetime semantics. Disabled state cannot silently serve fake ground truth.
2. Built-in daemon HSR AND Cell wiring demonstrably allocate/retain no historical recorder. Claims do not extend to direct default constructions or Tmux prematurely.
3. Interface cost is minimal and ownership is explicit. No generic outcome wrapper, new callback/reentry hazard, shallow forwarding hierarchy, or unnecessary public diagnostic API.
4. Regression and ruler plans use existing real objects and exact wire/observation evidence, distinguish live protocol state, and separate structural, sampled allocation, managed heap, and RSS claims.
5. At least two structurally distinct caller/type sketches are assessed honestly, with the losing shape's compatibility/complexity costs and a verifiable implementation slice.
