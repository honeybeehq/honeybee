Author one NEW disposable /tmp/honeybee-autotitle-transition-ruler.mjs for parent review. Keep quiet and retained rulers and all frozen original artifacts untouched. This is a bounded independent ruler task while Sol implements Unit2. Studio structural smoke only; no Mini, broad tests, production or repository edits.

Use the existing approved quiet ruler's provenance and fixture discipline: two immutable roots, explicit exact expected runtime delta for A/B, byte-identical distinct roots for A/A, same boot/tool/source checks, one fixture made with before module then verified copies, real store factory with unchanged signature, logical clocks, raw paired ABBA CPU/wall samples, separate diagnostic read counts, exact state/sidecar/outcome parity. Before-module helpers seed signatures. Assert full-field identity for any offline mailbox rows and label their audit omissions. No actual naming provider. Do not import or change the frozen quiet ruler since it self-executes.

Exactly four scenarios, bounded smoke sizes and the proposed canonical sizes recorded explicitly:
1. One changed bee each scan in a 1000-bee thin-deferred fleet. Append an envelope-only public message to a rotating bee outside the timed scan. The id membership changes but the normalized context/signature remains thin and quiet. This prices a realistic sparse miss without introducing unrelated generation work. Assert target rotation and all sides' exact state parity.
2. All reached bees changed each scan in the same fleet. Append one envelope-only public message per bee in one transaction outside timing. All signatures remain unchanged deferred, so every bee is reached, no probe-cap ambiguity. This is the adverse all-miss control and must show any added aggregate-query overhead honestly.
3. Repeated backoff expiry of one substantive giant. Seed 100k rows with interleaved pending/delivered history. Advance logical time by >600000 ms per measured cycle, so every scan must launch. Use a deterministic rejecting mock provider and drain completion outside scan timing; retain scan CPU/wall only, explicitly excluding mock-provider completion. Check exact provider context, retry count, sidecar and outcome stream after each drain, including unchanged initial task and last-three messages. Distinguish the first scan from warmed repeats. Fake-provider timing is not a production generation measurement.
4. A quiet fleet of 10 substantive backoff giants with 20000 interleaved rows each. A measured multi-giant stress shape, not an extrapolation. All ten must be visited and remain inside the seeded 600s backoff window. Fixture setup can use offline rows under the PK and audit proof rules; retain total rows and per-bee sizes in the report.

Use 30 measured scans per side after bounded warmup, ABBA ordering, with mutations/setup/drain/oracles outside clocks and enough recorded scheduling information to reconstruct each side's steps. New fixture copies per scenario, never reuse mutable templates. All failed attempts remain recorded. Diagnostics may wrap public read methods outside timing with pass-through interception, restore in finally, and assert counts to prove all expected bees were reached; no new production counters. Plan/source checks can be recorded as data without constraining an unknown final implementation.

Before writing code, read the current real autoTitle factory/policy and the selected synthesis. If any scenario's expected behavior is inconsistent with source, report the concrete issue and propose the smallest correction; do not relax assertions to make it run. Freeze syntax-checked ruler plus full four-case structural A/A smoke and hashes for parent review. No allocation or retained-memory features in this ruler.
