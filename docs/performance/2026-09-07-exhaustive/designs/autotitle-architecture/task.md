Produce one candidate DESIGN package for reducing repeated automatic-title mailbox-read CPU, without changing observable behavior. No production implementation or worktree edits. Read the architect skill and references/runner-prompt.md; use its rationale-template.md. Write usage first, type/signature sketch with unimplemented bodies/pseudocode, module map, rationale, concrete rejected alternatives, and test/measurement plan in your assigned directory. Consider at least two structurally distinct whole designs before choosing yours. Do not coordinate with the other candidate before freezing.

Grounding: /Users/trmd/Projects/trmd/honeybee/repos/honeybee-perf-exhaustive-2026-09-07/docs/performance/2026-09-07-exhaustive/autotitle-read-investigation.md and its linked original/corrected studies and parent uncommitted counterexample. Source baseline 604bf404. Read v2/daemon/src/autoTitle.ts, naming.ts, daemon.ts AUTO_TITLE_SCAN_MS call site, and actual CoreStore mutation/transaction/read seams. Core architecture contract applies. The work is ephemeral /tmp design, no new agents or Mini activity.

Facts to preserve: daemon scans at 1Hz; untitled-active does not imply small; deferred/backoff fleets currently re-read every scan outside the8-probe count; single global generator slot, at most one launch per scan, roster ordering, existing retry resets and45s watchdog. Signature counts nonempty JS-clamped texts and first clamped text; launch initialTask is separately stripped full text and last3 clamped user messages. Fixed first-k plus SQL COUNT(*) is refuted. Keep exact decision/context/bookkeeping semantics, including unchanged defer quietness and changed-context retry reset. No changes to detection latency or fairness in this unit.

Compare committed in-memory change detection, a different invalidation/summary design, and the status quo. Persisted sidecar fields/store schema are not pre-approved. Prefer no new persistent state and no generic cache framework. Any result reuse must prove transaction rollback safety: uncommitted AUTOINCREMENT ids can be reused with different bodies at equal(max,count), and CoreStore.inTransaction exists. Include deleteBee cascade, reopen/restart, reentry/custom deps, and mutable caller-provided Bee rows. Body/id immutability assumptions need exact scope. Count probes are O(index entries), not O(1); explain actual indexed SQL and body reads if proposing them. Retained state must be bounded and measured, not silently grow with total history. Avoid duplicating title policy across modules; daemon owns normalization/provider policy, core owns durable facts.

Produce a concrete design a reviewer can implement/test. Hypotheses are allowed but label unresolved measurement/semantic dependencies. No provider requests, full builds, broad tests or Mini runs; only cheap local scratch checks if essential. Freeze outputs with hashes and report.
