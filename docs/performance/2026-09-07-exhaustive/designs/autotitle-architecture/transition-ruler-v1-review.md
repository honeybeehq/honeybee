# Transition ruler draft review

Reviewed SHA-256 78d4020473f5d4ee9b9ea028497e4631abc96a1db3aa926d49d5cb2d5b63ee75. Revise before canonical. The four fixture meanings and copied-root discipline are appropriate. Preserve v1, the failed first smoke, and the passing smoke unchanged.

1. Fix throwingProvider's extra function layer. It currently returns () => () => throw, then generateFor returns that to the factory. A provider invocation therefore returns a function instead of throwing. Use exactly one generate function that increments a per-side invocation count and throws. Assert zero invocations at every quiet scenario end. Sidecar checks usually catch a launch, but the stated provider guard must itself work.
2. Canonical expiryMeasuredCycles=30 is iterated through ABBA, producing SIXTY measured samples per side. The agreed budget is thirty. Use 15 ABBA rounds, named consistently with the other scenarios, and assert actual raw sample counts and total launch/scan counts. Smoke's 14 launches currently reconciles to 1 first + 4 warm + 8 measured + 1 diagnostic. Record this accounting exactly after the revision.
3. Make expiry completion explicitly controlled outside the clock. generate currently constructs and returns Promise.reject during a microtask that runs before the await-dispatch continuation stops timing. Final rejection handling appears later in current code, but the claimed exclusion relies on microtask ordering. Have the fake provider return a held promise, record its context, stop the timed span, then reject exactly one pending promise outside timing and drain. State that provider entry/held-promise allocation can be part of awaited scan timing, while rejection construction/completion/drain are excluded. Assert provider count increases exactly once each cycle, not just equality between sides or the latest context.
4. The expiry code currently asserts parsed bookkeeping, while the handoff claims exact sidecar bytes. Assert the expected serialized one-Bee sidecar string each cycle, or narrow every bytes claim. Prefer the exact-byte assertion because the expected serialization already follows from the known seed and claimed fields. Also assert initial state hashes are equal across sides before expiry/stress measurements, rather than only unchanged relative to each side's own opening state.

The expiry diagnostic correction from one read to two reads per baseline cycle is valid. Keep its failed-attempt record. Separate launching-scan and drain-scan read counts would make mechanism interpretation clearer, but this is optional; aggregate cycle counts are correctly scoped already.

No fixture-size expansion, allocation machinery, production changes or Mini work requested. Run syntax checks and one structural four-case A/A smoke after the fixes. Preserve failures and freeze revised bytes for parent review.
