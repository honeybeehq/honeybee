Implement Unit2 only on top of clean committed bc6554b6 in honeybee-perf-autotitle-membership-2026-09-07. Parent source review found no blocker; Mini build, all v2 checks and Core 237/237 PASS on that exact commit. Independent review is in progress and any concrete finding will be forwarded. This is explicit authorization to continue implementation now.

Follow synthesis.md and synthesis-signatures.ts. Modify autoTitle.ts and meaningful daemon tests only. Preserve Unit1 source/tests and all other frozen lanes. Private store-backed no-argument cache only; custom dependencies and supplied rosters retain the original path and callback order. Cache membership plus exact signature, no history rows/context/normalized arrays. Committed before/read/normalize/signature/after pairing, with publication once before old branches. Reuse only unchanged deferred or exact active backoff. Every expiry, changed signature and potential generation retains full legacy context construction. Preserve truthy lastAt and share the original backoff condition rather than duplicate policy. Prune against the entire fresh active-untitled roster, including unvisited suffix, only on actual scanning calls. No public diagnostic API/property or persisted cache.

Tests must prove outcomes, contexts, state bytes and exact launch order in a real store. Include rollback/reused-id publication counterexample; scans while outer/nested tx open cannot consume prior cache or publish speculative data; mid-backoff mail change resets retries; delivery/urgency/unrelated output leaves contents unchanged; envelope-only changes and large envelope prefixes preserve decisions; expiry full read with unclamped initial task; zero lastAt; deletion/recreation; custom dependency callback order/reentry; supplied stale roster fallback; single slot/watchdog/probe cap. Cache-bound tests cannot use Reflect to access a closure variable. Use behavioral refill/pruning evidence or carefully scoped test-side instrumentation on existing operations, without exposing a new production seam. Bound is <= last full active-untitled roster, not equality and not immediate while disabled/in-flight.

Use a deterministic differential outcome-stream test where useful, comparing store-backed dispatcher against the unchanged general factory with equivalent real stores/state. Existing tests must remain unrelaxed. Keep tests practical; deterministic held/failing provider, no real provider or complex end-to-end runner.

Run focused autoTitle tests plus core/daemon typechecks, commit explicit paths and freeze exact production/test hashes with honest failed-attempt history. No Mini, broad tests or build from your lane. Parent owns whole-scan first/warm/changed/expiry/stress/retained measurements and final broad gates. Extra queries on misses and empty mailboxes remain measured tradeoffs; no optimization claim until those reports exist.
