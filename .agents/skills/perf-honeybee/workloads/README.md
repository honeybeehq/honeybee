# Performance map

Generated by `node scripts/perf-map.mjs index` from the JSON records in this directory. Do not edit by hand; edit the records.

Each workload is a `<id>.json` record (machine-readable, schema in `record.schema.json`) and a `<id>.md` interpretation. Metric status is a state, not a ladder: `gap` names a cost nobody can collect yet, `recipe-only` has a command but no retained receipt, `measured` has a receipt for the current owners, `stale` means an owner changed since the receipt, `gated` means a test asserts it.

| Workload | Lane | Processes | gap | recipe-only | measured | stale | gated | Unmeasured states | Readiness |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: | --- | --- |
| [Unchanged action lane reconciliation](./action-noop-reconciliation.md) | steady-state | hived | 0 | 0 | 0 | 0 | 1 | recovery | 18 deterministic samples prove zero full-lane audit view builds on unchanged polls; transition/reopen controls are tests, total scheduler cost remains unmeasured. |
| [Batch action-view projection](./action-view-projection.md) | throughput | hived | 0 | 0 | 0 | 0 | 1 | recovery | 45 count samples with exact view parity; 47 tests including filtered controls/reopen and exhaustive ordered status combinations. No CPU, latency or memory claim. |
| [Boot-state lookup over retained runtime history](./boot-runtime-lookup.md) | steady-state | hived | 0 | 0 | 0 | 0 | 2 | first-use, recovery | 150 paired count samples have boolean parity and zero stopped-history scan growth; all-live VM overhead is 5–125 instructions. Node plan/lifecycle controls pass separately. No timing claim. |
| [Remote Cell spawn: satellite owner](./remote-cell-spawn.md) | startup | hived, bee-runtime, helper | 3 | 3 | 0 | 0 | 0 | first-use, repeated, scale, release, recovery | Executable owner-side satellite stub benchmark; real provider, per-phase attribution and workstation-to-visible latency remain explicit gaps. |

## Accounted gaps (74)

Census keys no workload measures, each with a reason. Removing a reason moves the key to drift.

- `entry:v2/cli/src/help.ts:*` — v2/cli/src/help.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/cli/src/main.ts:*` — v2/cli/src/main.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/cli/src/render.ts:*` — v2/cli/src/render.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/cli/src/style.ts:*` — v2/cli/src/style.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/accountsService.ts:*` — v2/daemon/src/accountsService.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/activation.ts:*` — v2/daemon/src/activation.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/autoTitle.ts:*` — v2/daemon/src/autoTitle.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/config.ts:*` — v2/daemon/src/config.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/cursorAuth.ts:*` — v2/daemon/src/cursorAuth.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/daemon.ts:*` — v2/daemon/src/daemon.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/envelope.ts:*` — v2/daemon/src/envelope.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/gateways.ts:*` — v2/daemon/src/gateways.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/homeDefaults.ts:*` — v2/daemon/src/homeDefaults.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/import-probes.ts:*` — v2/daemon/src/import-probes.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/keychain.ts:*` — v2/daemon/src/keychain.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/login/claudeOauth.ts:*` — v2/daemon/src/login/claudeOauth.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/login/cliRunner.ts:*` — v2/daemon/src/login/cliRunner.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/login/common.ts:*` — v2/daemon/src/login/common.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/login/directKey.ts:*` — v2/daemon/src/login/directKey.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/login/transports.ts:*` — v2/daemon/src/login/transports.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/loginFlows.ts:*` — v2/daemon/src/loginFlows.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/loginWorker.ts:*` — v2/daemon/src/loginWorker.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/main.ts:*` — v2/daemon/src/main.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/naming.ts:*` — v2/daemon/src/naming.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/namingService.ts:*` — v2/daemon/src/namingService.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/performance.ts:*` — v2/daemon/src/performance.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/providerLimits.ts:*` — v2/daemon/src/providerLimits.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/relocateSession.ts:*` — v2/daemon/src/relocateSession.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/rpc.ts:*` — v2/daemon/src/rpc.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/service.ts:*` — v2/daemon/src/service.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/telemetry.ts:*` — v2/daemon/src/telemetry.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/threadCompactor.ts:*` — v2/daemon/src/threadCompactor.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/threadHistory.ts:*` — v2/daemon/src/threadHistory.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/daemon/src/tmuxHarness.ts:*` — v2/daemon/src/tmuxHarness.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/driver-cell/src/capture.ts:*` — v2/driver-cell/src/capture.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/driver-cell/src/cow.ts:*` — v2/driver-cell/src/cow.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/driver-cell/src/exec.ts:*` — v2/driver-cell/src/exec.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/driver-cell/src/git.ts:*` — v2/driver-cell/src/git.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/driver-cell/src/gitImage.ts:*` — v2/driver-cell/src/gitImage.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/driver-cell/src/layout.ts:*` — v2/driver-cell/src/layout.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/driver-cell/src/ledger.ts:*` — v2/driver-cell/src/ledger.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/driver-cell/src/remove.ts:*` — v2/driver-cell/src/remove.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/driver-cell/src/sandbox.ts:*` — v2/driver-cell/src/sandbox.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/driver-hsr/src/psutil.ts:*` — v2/driver-hsr/src/psutil.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/driver-hsr/src/runner-host.ts:*` — v2/driver-hsr/src/runner-host.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `socket:v2/driver-hsr/src/runner-host.ts#*` — v2/driver-hsr/src/runner-host.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `spawn:v2/cli/src/main.ts#*` — v2/cli/src/main.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `spawn:v2/daemon/src/daemon.ts#*` — v2/daemon/src/daemon.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `spawn:v2/daemon/src/import-probes.ts#*` — v2/daemon/src/import-probes.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `spawn:v2/daemon/src/loginFlows.ts#*` — v2/daemon/src/loginFlows.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `spawn:v2/daemon/src/loginWorker.ts#*` — v2/daemon/src/loginWorker.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `spawn:v2/daemon/src/naming.ts#*` — v2/daemon/src/naming.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `spawn:v2/daemon/src/namingService.ts#*` — v2/daemon/src/namingService.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `spawn:v2/daemon/src/threadCompactor.ts#*` — v2/daemon/src/threadCompactor.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `spawn:v2/driver-cell/src/cow.ts#*` — v2/driver-cell/src/cow.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `spawn:v2/driver-cell/src/exec.ts#*` — v2/driver-cell/src/exec.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `spawn:v2/driver-cell/src/git.ts#*` — v2/driver-cell/src/git.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `spawn:v2/driver-cell/src/gitImage.ts#*` — v2/driver-cell/src/gitImage.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `spawn:v2/driver-hsr/src/psutil.ts#*` — v2/driver-hsr/src/psutil.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `spawn:v2/driver-hsr/src/runner-host.ts#*` — v2/driver-hsr/src/runner-host.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `timer:v2/cli/src/client.ts#*` — v2/cli/src/client.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `timer:v2/cli/src/main.ts#*` — v2/cli/src/main.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `timer:v2/daemon/src/accountsService.ts#*` — v2/daemon/src/accountsService.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `timer:v2/daemon/src/daemon.ts#*` — v2/daemon/src/daemon.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `timer:v2/daemon/src/loginWorker.ts#*` — v2/daemon/src/loginWorker.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `timer:v2/daemon/src/namingService.ts#*` — v2/daemon/src/namingService.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `timer:v2/daemon/src/performance.ts#*` — v2/daemon/src/performance.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `timer:v2/daemon/src/service.ts#*` — v2/daemon/src/service.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `timer:v2/daemon/src/threadCompactor.ts#*` — v2/daemon/src/threadCompactor.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `timer:v2/daemon/src/threadOperations.ts#*` — v2/daemon/src/threadOperations.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `timer:v2/driver-cell/src/exec.ts#*` — v2/driver-cell/src/exec.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `timer:v2/driver-cell/src/provisionWorker.ts#*` — v2/driver-cell/src/provisionWorker.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `timer:v2/driver-hsr/src/driver.ts#*` — v2/driver-hsr/src/driver.ts: outside the spawn readiness assertions; exports/timers include recovery, maintenance or CLI paths. Add a workload before claiming these costs are covered.
- `entry:v2/driver-cell/src/provision.ts:provisionRequestOf` — Ledger reconstruction for replay is outside fresh-spawn timing.
